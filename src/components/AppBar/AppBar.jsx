const styles = {
  header: {
    margin: '0 auto',
    minHeight: 80,
  },
};

const AppBar = () => (
  <header style={styles.header}>
    <h1>HW-06(7)-phonebook</h1>
  </header>
);

export default AppBar;
